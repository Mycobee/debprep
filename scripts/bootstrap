#!/bin/bash
set -euo pipefail

SERVER_IP=${1:-}
SSH_PRIVATE_KEY_PATH=${2:-}

if [[ -z "$SERVER_IP" || -z "$SSH_PRIVATE_KEY_PATH" ]]; then
	echo "Usage: $0 <server_ip> <ssh_private_key_path>" >&2
	exit 1
fi

./scripts/init "$SERVER_IP" "$SSH_PRIVATE_KEY_PATH"
./scripts/configure
